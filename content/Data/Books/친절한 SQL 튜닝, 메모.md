---
created: 2026-02-03T10:40:02+09:00
modified: 2026-02-03T15:12:14+09:00
---
- 파일 경합을 줄이기 위해 한 테이블의 익스텐트여도 여러 파일에 나누어 저장합니다.
- db_file_multiblock_read_count
	- multiblock I/O를 할 때 한번에 몇 개의 블록을 가져올 지 결정합니다.
- Multiblock I/O는 인접한 블록 여러 개를 한번에 가져오는데, 이는 같은 익스텐트의 데이터를 의미합니다.
- 버퍼 캐시는 Hash 구조로 관리합니다.
- 한 블록에 동시에 접근은 불가능합니다. 래치를 이용해 직렬화 합니다.
- 버퍼 체인을 탐색하는 동안 래치로 잠그고, 원하는 블록 발견 시 해제 합니다.
	- 이후 블록에 대한 Lock 실행
- LMC
- OR Expansion
- 스칼라 서브쿼리 
- Index Full Scan은 Multiblock I/O인가?
	- Singleblock I/O 이다. 다만 Index Fast Full Scan은 Multiblock I/O입니다.
- Unique 스캔
	- 인덱스가 Unique라면 유니크이기 때문에 원하는 값을 찾으면 끝입니다.
	- 모든 조건을 다 써야합니다.
- Skip Scan
	- 선두 컬럼이 없거나, 선두 컬럼은 존재하지만 중간 컬럼이 없을 때에도 사용 가능합니다.
	- 선두 컬럼이 Between, Like 같은 범위 조건이어도 사용 가능
- Index Fast Full Scan
	- 인덱스 트리구조를 무시하고 세그먼트 전체를 Multiblock I/O 합니다.
	- 순서를 상관하지 않고 읽기 때문에 정렬이 보장되지 않습니다.
	- 파티션이 아니어도 병렬 처리 가능합니다.
- 논리적 주소, 물리적 주소의 차이
- 인덱스 ROWID는 논리적 구조
- **130p~135p**, 메인 메모리 DB와의 비교 
- 인덱스 클러스터링 팩터
	- 군집성 계수
	- 특정 컬럼 내에 같은 값을 갖는 데이터가 모여있는 정도
	- 직전에 읽은 테이블 블록 포인터를 해제하지 않고(버퍼 pinning), 다음으로 찾은 블록이 같은 블록이라면 래치 획득과 해시 체인 스캔을 생략할 수 있습니다.
	- 인덱스 손익분기점은 5~20% 정도이지만, 클러스터링 팩터에 따라서 크게 달라집니다.
- DW / OLAP / 배치
- **143p**, 하단
- 테이블을 파티셔닝 하는 이유는 테이블 풀 스캔을 빠르게 하기 위함 입니다.
- 인덱스 구조 테이블, IOT 'Index organized table'
- **153p**, 데이터를 입력할 때, 랜덤 방식
- 클러스터 테이블
	- 인덱스 클러스터
		- 클러스터 키 값이 같은 데이터를 한 블록에 모아서 저장
		- 인덱스 하나에 여러 개 데이터가 있어서 비효율이 없다.
	- 해시 클러스터
		- 해시를 통해 데이터가 묶임
- 부분 범위 처리
	- 앞의 몇 개만 필요하다면 Array Size를 작게, 다 필요하다면 크게 합니다.
- 배치 I/O
	- 버퍼 캐시에서 블록을 못 찾았을 때, 바로 찾으러 가는게 아니라 일정량이 쌓이면 디스크에 접근합니다.
	- 한 번에 몰아서 가져오기 때문에 데이터 정렬이 다를 수 있습니다.
	- 정렬 순서를 보장할 수 없기 때문에 Sort Order By가 추가 됩니다.
	- 소트를 생략할 수 없는 경우에는 안 쓸 이유가 없습니다.
- 부분범위처리와 ROWNUM과의 관계는?
- 173p, 실제 데이터는 리프노드에만?
	- 하위 노드를 가리키는 블록주소
- 185p, 인덱스의 액세스 조건, 필터 조건, 테이블 필터 조건
	- 선행 조건이 모두 '='일 때 까지는 군집
- 190p, 몇 가지 케이스
- 조건을 만족하는 레코드가 더 없음을 확인하는 동작을 one-plus 스캔이라고 합니다.
- Between을 In-List로 전환하는 경우
	- 195p, In-List Iterator
	- 각각을 = 으로 변경하여 탐색 후 UNION ALL
	- 항상 좋지만은 않음, In-List의 개수가 많으면 수직적 탐색 때문에 오히려 비효율적일 수도 있습니다.
	- 198p, 레코드가 붙어있을 때는 또 다릅니다.
	- 비슷한 경우는 Index Skip Scan
- NUM_INDEX_KEYS
	- 고객번호+상품ID 인덱스를 가지고 있고, 둘다 조건절에 있지만 고객번호만 액세스 조건으로 사용하고 싶을 때
	- 208p, RTRIM으로 In-List 만들기?
- Between과 Like는 조건에 따라 스캔 범위가 다릅니다.
	- Between이 번거로워도 손해 볼 일은 없습니다.
- 옵션 처리 장단점 비교
	- 215p, Or Expansion은 왜 자동으로 안일어나나?
	- 214p, Or은 웬만하면 사용하지 않는게 좋습니다.
- Between, Like
	- 필수조건 컬럼을 선두에 둘 때, 쓸만합니다.
	- 217p, 필수조건이 변별력이 적다면?
	- Null은 Between, Like에 포함되지 않습니다.
- 221p, UNION ALL 사용
	-  효율적입니다. 길다는 것 빼고는 좋습니다.
- 225p, PL/SQL
	- 보통 느립니다. 그 이유는 다음과 같습니다.
		- 가상 머신에서 실행되는 인터프리터 언어
		- 호출 시 마다 컨텍스트 스위칭
		- 내장 SQL에 대한 Recursive Call 발생
	- PL/SQL 함수는 실행 시 매번 SQL 실행 엔진과 PL/SQL 가상 머신과의 컨텍스트 스위칭이 일어납니다. 
	- PL/SQL이 느린 가장 큰 이유는 Recursive Call 입니다.
- **226p**, select에 서브쿼리가 있을 때
- TPS란?
	- 데이터가 1초 동안 처리할 수 있는 트랜잭션의 수
- **232p**, 인덱스 설계 판단 기준
- **245p**, 중복 인덱스 제거
- NL 조인
	- **260p**, 실행 계획
	- 264p, 튜닝 포인트
	- 특징
		- 랜덤 액세스 위주의 조인방식
		- 한 레코드씩 순차적 진행 ( 큰 테이블에서 부분 처리 시 매우 빠름 )
		- 인덱스 구성 전략이 중요
	- 결론
		- 소량 데이터를 주로 처리하거나, 부분 범위 처리가 가능한 OLTP 시스템에 적합
	- 조인하려는 테이블에 인덱스가 없으면 사용이 불가능한가?
- 테이블 prefetch
	- 디스크 I/O를 할 때 곧 읽게 될 블록도 미리 읽어와 버퍼 캐시에 적재
- 배치 I/O
	- 디스크 I/O를 쌓아놓고 일정 량이 쌓이면 한 번에 처리
- 270p, 각각의 실행계획 예시
- NL 조인 시 Inner 조건을 왼쪽에 두는게 좋습니다.
- 274p, 소트 머지 조인
	- 대량의 데이터를 다룰 때 좋습니다.
	- 275p, SGA와 PGA에 대한 설명과 그림을 볼 수 있습니다.
	- 소트 머지 조인 과정 (e와 c를 조인한다고 할 때)
		1. e의 조건이 맞는 데이터를 찾아 조인 조건으로 정렬하고 PGA에 저장
		2. c도 동일하게 조건에 맞는 데이터를 찾고 조인 조건으로 정렬하여 PGA에 저장
		3. PGA에서 e를 보며 c와 맞춰보며 조인!
	- 278p, 소트 머지 조인이 빠른 이유
	- 주 용도
		- 조인 조건이 등치가 아닐 때, 대량 조인
		- 조인 조건이 아예 없는 조인(크로스 조인, 카테시안 곱)
	- 280p, 실행 계획
	- 조인 컬럼만 PGA에 넣는 것이 아니라 해당 row는 전부 넣습니다.
	- PGA를 사용하다 부족하면 Temp 스페이스를 사용합니다.
- 282p, 해시 조인
	- Build와 Probe
		- Build - 작은 쪽 테이블을 해시 함수로 하여 해시 테이블 생성 (PGA저장)
		- Probe - 큰 쪽 테이블을 읽어 해시 테이블을 탐색하며 조인
	- Build 시 조인 컬럼을 테이블 키로 사용합니다.
	- PGA의 Hash Area를 사용하다 부족하면 Temp 스페이스를 사용합니다.
	- 해시 조인이 빠른 이유
		- PGA에 값을 저장하기 때문
	- 해시 조인이 머지 소트 조인보다 빠른 이유는?
		- PGA에 저장하는 데이터 양의 차이
			- 머지소트조인 -> 양쪽 집합 전부를 정렬해서 담는다.
			- 해시조인 -> 둘 중 작은 집합만 넣는다.
		- Temp를 쓰게 되더라도 일반적으로 가장 빠름
	- 두 테이블이 모두 클 때 해시조인은 어떻게?
		- 분할 정복을 사용합니다.
			1. 두 테이블 모두 조인 컬럼에 해시를 적용해 해시 값으로 파티셔닝 합니다. 여기서 같은 해시 값이 나온 테이블1과 테이블2의 파티션을 페어라고 부릅니다.
			2. 각 페어끼리 조인합니다. 여기서도 작은 파티션이 build, 큰 파티션이 probe를 합니다. 기존 테이블의 크기와는 상관 없습니다.
	- 290p, 해시 조인 실행 계획
	- 힌트에서 가장 먼저 나타난 테이블을 Build input으로 정하지만, swap_join_inputs(c)를 통해 직접 지정 가능합니다.
	- no_swap_join_inputs(c)를 지정해서 절대 input이 되지 않게 할 수 있습니다.
- 296p, 조인 메소드 선택 기준
- 서브쿼리
	- no_unnest
		- 서브쿼리를 풀지 않고 그대로 필터 조건으로 사용합니다.
	- 303p, 서브쿼리를 필터로만 사용
	- 304p, 필터는 캐싱 기능을 가집니다.
	- 서브쿼리 unnesting 중첩을 풀어냅니다.
		- 보통은 옵티마이저가 알아서 선택합니다.
	- 305p, 서브쿼리를 unnest하고 쿼리 id를 주어서 메인 쿼리보다 먼저 처리되게도 가능합니다.
	- 307p, ROWNUM은 병렬쿼리나 서브쿼리에 잘못쓰면 독이 됩니다.
	- 인라인 뷰를 꺼내어 합칩니다. merge
- 313p, 조인조건 pushdown
- 315p, 인라인 뷰에서 바깥쿼리 컬럼 참조
- 스칼라 서브쿼리와 함수의 차이
	- 스칼라 서브쿼리는 조인을 이용한 것과 같고, 캐싱 처리가 됩니다.
	- 319p, 사용자 정의 함수를 스칼라 서브쿼리로 덧 씌우면 캐시를 이용하게 되어 빨라집니다
- 323p, 스칼라 서브쿼리에서 여러 컬럼이 필요하면, 모두를 이어붙인 문자열을 반환한 뒤, 메인쿼리에서 잘라서 씁니다.
- 324p, 서브쿼리와 인라인 뷰도 조인 조건 push down을 이용하면 괜찮습니다.
- 326p, 다시 한번 확인해보기
- 331p, 소트
- 정렬된 결과를 얻고 싶다면, 실행 계획에 Sort Group By가 있더라도 반드시 Order By를 해줘야 합니다.
- 339p, Sort Unique는 join 하기 전에 중복제거 하는 작업을 말합니다.
- 343p, UNION ALL, UNION
- 346p, DISTINCT와 Exist
	- 348p, distinct와 minus 연산자를 이용한 쿼리는 대부분 Exist로 변환이 가능합니다.
- 349p, Hash Join은 소트 연산의 생략이 불가능한가?
	- NL 조인으로 하면 생략 가능합니다.
- 350p, 인덱스를 이용한 sort 생략 